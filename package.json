{
  "name": "wiser-traffic-analyzer",
  "version": "2.0.0",
  "description": "Wiser Traffic Analyzer — GA4 OAuth2, one-click deploy",
  "main": "server/index.js",
  "scripts": {
    "start": "node server/index.js",
    "dev": "nodemon server/index.js",
    "build": "cd client && npm install && npm run build",
    "setup": "npm install && npm run build",
    "deploy:railway": "echo 'Push to GitHub then connect repo on railway.app'",
    "deploy:render": "echo 'Push to GitHub then connect repo on render.com'"
  },
  "dependencies": {
    "axios": "^1.6.0",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "express-session": "^1.17.3",
    "googleapis": "^140.0.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
